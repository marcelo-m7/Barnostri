name: barnostri_app
description: "A Moninhay project"
publish_to: 'none'
version: 1.0.0

environment:
  sdk: ">=3.8.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter    # puxa intl 0.20.2
  # Se você realmente usa Intl diretamente, deixe esta linha
  intl: 0.20.2

  supabase_flutter: ^2.9.1        # mesma major que shared_models
  cached_network_image: ^3.0.0
  qr_code_scanner:
    path: ../../packages/qr_code_scanner
  image_picker: '>=1.1.2'
  cupertino_icons: ^1.0.0
  fl_chart: ^1.0.0
  google_fonts: ^6.2.1
  shared_preferences: ^2.5.3
  path_provider: ^2.1.5
  http: '>=1.0.0'
  uuid: '>=3.0.0'
  flutter_riverpod: ^2.4.0
  go_router: ^16.0.0
  shared_models:
    path: ../../packages/shared_models

dev_dependencies:
  flutter_test:
    sdk: flutter
  change_app_package_name: ^1.4.0
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  generate: true
  assets:
    - supabase/supabase-config.json
